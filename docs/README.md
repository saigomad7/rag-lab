# docs — 점검 · 설계 문서

HTML 파일은 **더블클릭하면 브라우저에서 바로 열립니다**(스크립트·외부 연결 없음, 휴대폰에서도 열림).
xlsx 는 엑셀에서 열어 직접 고치며 씁니다.

## 먼저 볼 것

| 파일 | 무엇 |
|---|---|
| `rag_run_guide_rev1.xlsx` | **★ 실행 가이드** — 노트북 연습 순서 · 사내 실행 순서(통과 기준 · 기록 위치) · 노트북 14개 상세 · 설정 기입표 · 안 돌리는 파일 (6시트) |
| `rag_runbook_t2_rev1.html` | **★ T2 보고서판 (도식 13장)** — 전체 흐름 · 테이블 구조 · 검색 파이프라인 · 실패 3분류 · 원인 판별 흐름도 · 골든셋 3종 · 지표가 붙는 자리 · 노드 그래프 · 프리셋 계단 · 3주 일정 · 용어 12 |
| `rag_runbook_rev1.html` | **실무 안내서(원본)** — 전체 그림 · 준비 · 노트북 8단계 실행 순서 · 골든셋 만드는 법 · 지표 읽는 법 · LangGraph 고도화 · 자주 막히는 곳 · 파일 지도 |
| `rag_project_log_rev1.xlsx` | **★ 이력 대장** — 진행 이력 · 산출물 · 파이프라인 점검 · 골든셋 현황 · 실험 이력 · 프리셋 비교 · 지표 현황 · 이슈 · 다음 할 일 (10시트) |
| `rag_checklist_sheet_rev9.html` | **★ 진행 기준** — 스프레드시트 형식 체크리스트. 요약 · 소스별 매트릭스 · 소스별 상세 · **소스별 컬럼 정의** · 소스별 처리 흐름 · P1 적재/검색 · P2 센싱 · P3 정형연계 · 정형연계 흐름 · 스모크 20 |
| `rag_checklist_rev10.xlsx` | 위 체크리스트의 **엑셀판** — 상태 드롭다운, 요약 자동 계산(수식). 사내에서 채워 가며 쓰는 용도 |
| `rag_checklist_rev10.xlsx` 의 `단계별_실행도구` · `소스별_컬럼정의` · `JSON_스키마` 시트 | **소스별 컬럼 정의서** — 필요한 항목 98개(공통 20 · 본문/요약 14 · 청크 12 · 소스별 52 · 소스 7종)와 **저장 위치**(공통 컬럼 / META_EXTRA JSON / TAGS / ACL 행). K·L 열(사내 보유 · 사내 컬럼명)을 채우면 사내 테이블 매핑표. JSON_스키마 시트에 실제 JSON 모양과 매핑 4가지 경우 |
| `metric_catalog_template_rev1.xlsx` | **지표 정의서 양식**(S22) — 채워서 `catalog/metric_catalog.xlsx` 로 저장하면 `nb07_sql_router.py` 가 읽음 |

## 이해용

| 파일 | 무엇 |
|---|---|
| `query_flow_example_rev2.html` | 질의 1건이 **비정형(문서) + 정형(DB)** 을 함께 도는 과정 + 부록 "지표 정의서는 이렇게 만든다" |
| `smoke_test_20_rev1.html` | 스모크 테스트 20문항과 실패 유형 5종 · 집계표 (S07-1) |
| `golden_eval_pack_rev4.xlsx` | **★ 최신 · 사내 기입용** — **`1b_LLM골든셋_검수`**(nb12 생성분 검수 · 자가검증) · `3_평가_실행기록` 프리셋 열 + **`2d_근거문서_연결`**(질문↔코퍼스 근거 확정 · 근거 확보율) · **`2e_원본_수집목록`**(원본 URL·수집·파싱·청킹·인덱싱 상태 대장) · 안내에 측정 성립 조건 · 실패 3분류 추가 |
| `golden_set_example_rev2.xlsx` | **골든셋 실물 예시 9종** — **LLM 생성 골든셋(nb12)** · **근거 문서 연결(nb11)** · 수동 · 자동 평가셋 3종 · 정형 · 스모크 20 (샘플 데이터로 실제 생성한 값) |
| `rag_design_guide_rev4.html` | 설계 원본 — 아키텍처, 결정 D1~D6, Oracle · Milvus 스키마, Phase 2 센싱 설계 |
| `rag_quest_board_rev4.html` | 항목별 **옵션 · 장단점 · 완료 조건** 사전 (Q00~Q40) |
| `rag_stage_checklist_rev3.html` | 단계별 체크리스트 상세판 (S01~S21, 103항목) |

## src/

문서를 만든 파이썬 스크립트입니다. 내용을 고칠 때는 데이터 파일(`sources_data.py`, `flows_data.py`, `phase3_data.py`)만 고치고 `make_*.py` 를 다시 실행하면 같은 모양으로 다시 만들어집니다. (pandas · openpyxl 필요)

## 단계 번호

- **S01~S06** 적재(수집 · 파싱 · 청킹 · 임베딩 · 메타 · 벡터DB) · **S07~S15** 검색(질의 · BM25 · 시맨틱 · 결합 · 리랭크 · 컨텍스트 · 답변 · 평가)
- **S16~S21** 마켓 센싱(Phase 2) · **S22~S29** 정형 데이터 연계(Phase 3)
- 코드(`nb01`~`nb07`)가 각 단계의 현 수준을 실제 값으로 채웁니다.

> 문서 안의 수치 · 회사명 · 문서 예시는 모두 **설명용으로 지어낸 값**입니다.
